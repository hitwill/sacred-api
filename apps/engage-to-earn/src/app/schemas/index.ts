import { content } from './content.schema';
import { rewards } from './rewards.schema';
import { user } from './user.schema';
import { votes } from './votes.schema';

export const Schema = {
  content,
  rewards,
  user,
  votes,
};
